<!-- header.ejs -->

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Scatch</title>
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet" />
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="font-['helvetica_now_display']">
    <nav class="w-full flex justify-between px-5 py-3 items-center">
        <h3 class="text-xl">Scatch</h3>
        
        <% if (isAuthenticated) { %>
            <div class="flex gap-5 items-center"> <!-- Added items-center for vertical alignment -->
                <a href="/shop" class="hover:text-gray-700">Shop</a>
                <a href="/products/cart" class="hover:text-gray-700 flex items-center gap-1">Cart</a>
                    
                  
                <a href="#" class="hover:text-gray-700">My Account</a>
                <a class="text-red-600 hover:text-red-800" href="/users/logout">Logout</a>
            </div>
        <% } %>
    </nav>
</body>
</html>
