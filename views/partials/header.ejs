<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Scatch</title>
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet" />
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        /* Navbar styles */
        nav {
            background-color: #f9f9f9; /* Background color for the entire navbar */
        }
        .navbar {
            list-style-type: none;
            margin: 0;
            padding: 0;
            display: flex;
            gap: 16px; /* Space between items */
        }
        .navbar li a {
            display: block;
            color: white; /* Default link color */
            padding: 6px 12px; /* Reduced padding for links */
            text-decoration: none;
        }
        /* Change the link color on hover */
        .navbar li a:hover {
            background-color: #555; /* Background on hover */
            color: white; /* Text color on hover */
        }
        .brand {
            color: white; /* Set text color for the brand */
            text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.7); /* Adding a subtle shadow for depth */
        }
        .logout-link {
            color: red; /* Set base color for Logout link */
        }
        .logout-link:hover {
            color: red; /* Keep the color red on hover */
        }
    </style>
</head>
<body class="font-['helvetica_now_display'] bg-gray-100">
    <nav class="w-full shadow-md bg-[#282a35] sticky top-0 z-10"> <!-- Sticky navbar -->
        <div class="flex justify-between items-center p-2"> <!-- Set padding for the navbar -->
            <h3 class="text-2xl font-bold brand">Scatch</h3> <!-- Navbar title -->
            
            <% if (isAuthenticated) { %> <!-- Display links only if user is authenticated -->
                <ul class="navbar"> <!-- Navbar links -->
                    <li><a href="/shop" class="text-white hover:text-gray-300">Shop</a></li> <!-- Shop link -->
                    <li>
                        <a href="/products/cart" class="flex items-center gap-1 text-white hover:text-gray-300">
                            <i class="ri-shopping-cart-line"></i> Cart <!-- Cart link with icon -->
                        </a>
                    </li>
                    <li>
                        <a href="/users/logout" class="logout-link">Logout</a> <!-- Logout link with custom class -->
                    </li>
                </ul>
            <% } %>
        </div>
    </nav>
</body>
</html>
