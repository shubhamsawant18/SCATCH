<%- include('./partials/header') %>
<div class="w-full h-screen flex items-start px-20 py-20">
    <div class="w-[25%] flex h-screen flex-col items-start">
        <div class="flex flex-col">
            <a class="block w-fit mb-2" href="/owners/products/create">Create New Product</a>



        </div>
    </div>
    <div class="w-[75%] flex flex-col gap-5 h-screen">
        <a class="text-red-500" href="/owners/products/delete">Delete All</a>
        <div class="flex items-start gap-5 flex-wrap">
            <% products.forEach(product => { %>
                <div class="w-60 bg-red-500">
                    <div class="w-full h-52 bg-yellow-500"></div>
                    <div class="flex justify-between items-center px-4 py-4">
                        <div>
                            <h3><%= product.name %></h3>
                            <h4>₹ <%= product.price %></h4>
                        </div>
                        <a class="w-7 h-7 flex items-center justify-center rounded-full bg-white" href="/owners/products/add/<%= product.id %>">
                            <i class="ri-add-line"></i>
                        </a>
                    </div>
                </div>
            <% }) %>
        </div>
    </div>
</div>
<%- include('./partials/footer') %>
